const initialState = {
    categories: []
}

function CategoriesReducer(state = initialState, dispatch) {
    switch(dispatch.type) {
        default:
            return state;
    }
}

export default CategoriesReducer;

